# repo-scoped aws creds + personal launcher — never commit
.aws-local/
awsx

# institutional data pulled from s3 — do not commit
materials/

# scholarship exports — real applicant essays, never commit
*.csv
*.xlsx

# personal project notes + local claude config (agents, skills, api keys) — local only
CLAUDE.local.md
.claude/

# node / monorepo build junk
node_modules/
dist/
build/
*.tsbuildinfo
.env
.env.local

# python (api)
.venv/
__pycache__/

.DS_Store